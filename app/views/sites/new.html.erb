<h1>Want to get people together?</h1>

<h2>Put your group's blogs, tweets, and more all on one page. Create a shared site in 30 seconds.</h2>

<div id="example"><a href="http://planetinformatics.gathrr.com">see an example!</a></div>

<div class="center_form">
<script type="text/javascript">
  function clean_slug() {
    if ($('slug').value.match(/[^-_a-z0-9]/)) {
      $('slug').value = $('slug').value.replace(/[^-_a-z0-9]/g, '');
    }
  }
</script>
<% form_for(@site) do |f| %>
  <%= f.error_messages %>


  <div class="field">
    <%= f.label :slug, 'URL' %><br />
    <div class="field_outer">
    <div class="field_inner">
      <%= f.text_field :slug, :class => 'url', :id => 'slug', :onKeyUp => 'javascript:clean_slug();' %>.gathrr.com
    </div>
    </div>
  </div>

  <div class="field">
    <%= f.label :feed_list, 'Feeds' %><br />
    <div id="feed_examples">
      <p>Enter RSS feeds:<br>
      http://whitehouse.gov/feed/blog</p>

      <p>And twitter usernames:<br>
      @TheOnion</p>

      <p>... one per line!</p>
      
    </div>

    <div class="field_outer" id="feed_entry">
    <div class="field_inner">
      <%= f.text_area :feed_list %>
    </div>
    </div>
  </div>

  <p>
    <%= f.submit "Gather 'em" %>
  </p>
<% end %>

</div>
